# 🧾 Release Notes — Sprint 1 (Backend)

**Data:** 2025-10-30  
**Versão:** v1.0.0-sprint1  
**Responsável:** Equipe Backend / P.O: Rômulo Canella

---

## 🚀 Entregas
- API base (Express + TypeScript, padrão ESM/NodeNext).  
- Auth JWT, rotas: `/auth`, `/orders`, `/config`, `/products`, `/health`.  
- Testes de integração (Vitest + Supertest) cobrindo fluxos felizes e inválidos.  
- Coverage ≥90% linhas / ≥80% branches (gates no CI).  
- OpenAPI atualizado e validado no pipeline.  
- Rastreabilidade (REQ ↔ API ↔ Testes) — `map.json` + `matrix.md`.  
- Dockerfiles multi-stage (dev/test/prod) e `docker-compose` com serviços isolados.  
- `.env.example` publicado com variáveis padrão (`DATABASE_URL`, `JWT_SECRET`).  
- Build reprodutível + migração para ESM (NodeNext).

---

## 💡 Observações
- Auth simples com roles (`admin` / `oper`).  
- Health-check para CI e monitoramento (`GET /health`).  
- Scripts padrão (`build`, `start`, `test`, `lint:openapi`).

---

## 📈 Próxima Sprint (2)
- Persistência real via Prisma + PostgreSQL (pedidos/config).  
- Schemas `zod` sincronizados com OpenAPI.  
- Testes E2E (Playwright).  
- Integração frontend (Admin ↔ Loja).  
- Gates de cobertura obrigatórios no pipeline.  
